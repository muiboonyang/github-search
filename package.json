{
  "name": "github-search",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "backend/dist/app.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "npx kill-port 3001 5002 && concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" --kill-others",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build"
  }
}
